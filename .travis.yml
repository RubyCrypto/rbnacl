before_install:
 - sudo sudo add-apt-repository -y ppa:hauke/dnscurve
 - sudo apt-get update -qq
 - sudo apt-get install -qq libnacl-dev

rvm:
  - 1.8.7
  - 1.9.3
  - ree
  - ruby-head
  - rbx-18mode
  - rbx-19mode 

matrix:
  allow_failures:
    - ruby-head
